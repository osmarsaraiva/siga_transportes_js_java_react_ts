git init

git config --global user.name "Osmar Saraiva Jr"

git config --global user.email "osmar.saraiva@gmail.com"

git status

git add . para serem versionados

git commit -m "criação do projeto"

git log - historico

git remote add origin https://github.com/osmarsaraiva/siga-transportes_spring_js.git (vincular projeto PC e Web)

git push -u origin master



______________________________________

depois pode dar

git add . para serem versionados

git commit -m "mensagem"

git push 

git pull 
Atualizar os arquivos no branch atual



